{% extends 'template.html.twig' %}

{% block left %}
    <h3>Edit record for {{ brand.getName }}</h3><br>

        <form action='/brands/{{ brand.getId }}' method='post'>
            <div class='form-group'>
                <input name='_method' value='patch' hidden>
                <input type='text' name='brand_name' class='form-control' value='{{ brand.getName }}'>
            </div>

            <button type='submit' class='btn btn-primary'>Submit Edit</button>
        </form><br><br><br>

        <h4>Delete {{ brand.getName }} from records</h4>
        <h5>(Cannot be undone!)</h5>

        <form action='/brands/{{ brand.getId }}' method='post'>
            <input name='_method' value='delete' hidden>
            <button type='submit' class='btn btn-danger'>Delete Brand</button>
        </form>
{% endblock %}

{% block right %}

{% endblock %}
